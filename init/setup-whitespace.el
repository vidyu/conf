;;; package --- The config for whitespace
;;; Commentary:
;;; Code:

(global-set-key (kbd "C-c w w") 'whitespace-mode)
(global-set-key (kbd "C-c w c") 'whitespace-cleanup)

(provide 'setup-whitespace)
;;; setup-whitespace.el ends here
